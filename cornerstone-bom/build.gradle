plugins {
	id 'io.cornerstone.java-library-conventions'
}

dependencies {
	constraints {
		rootProject.subprojects.findAll { it.name != 'showcase' && !it.name.endsWith('-bom') }.each {
			api(group: it.group, name: it.name, version: it.version.toString())
		}
	}
}
